import 'package:flutter/material.dart';

class SeriesDetailPlayer extends StatelessWidget {
  const SeriesDetailPlayer({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder for video player - will be integrated with youtube_player_flutter
    return const SizedBox.shrink();
  }
}
